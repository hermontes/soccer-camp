export const metadata = {
  title: "Payment Failed - Summer Camp",
  description:
    "Your Summer Soccer Camp registration payment could not be processed.",
};

export default function FailureLayout({ children }) {
  return children;
}
